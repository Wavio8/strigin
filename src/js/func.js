export const toggleClass = (htmlEl, className) => {
  if (htmlEl.classList.contains(className)) {
    htmlEl.classList.remove(className);
  } else {
    htmlEl.classList.add(className);
  }
};

export const a = "";
